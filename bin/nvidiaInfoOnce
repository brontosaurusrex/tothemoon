#!/bin/bash

# nvidiaInfoOnce

h=$(hostname)
u=$(uptime -p)

bmiltime() {
	echo "$(date +%H%M%^b%d%y)"
	}

t=$(bmiltime)
echo "$h, $t, $u"
nvidia-smi --query-gpu=index,power.draw,clocks.sm,fan.speed,temperature.gpu --format=csv
